name: Build Cordova Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 3️⃣ Setup Java 17 (MANDATORY)
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # 4️⃣ Install Gradle 7.6.4 (REQUIRED)
      - name: Install Gradle 7.6.4
        run: |
          wget https://services.gradle.org/distributions/gradle-7.6.4-bin.zip
          unzip gradle-7.6.4-bin.zip
          sudo mv gradle-7.6.4 /opt/gradle
          echo "/opt/gradle/bin" >> $GITHUB_PATH
          gradle -v

      # 5️⃣ Install Cordova
      - name: Install Cordova
        run: npm install -g cordova@12

      # 6️⃣ Install project dependencies
      - name: Install dependencies
        run: npm install

      # 🔹 Install Android SDK Build Tools (BEFORE platform add)
      - name: Install Android SDK Build Tools
        run: |
          yes | sdkmanager "platform-tools" "platforms;android-32" "build-tools;32.0.0"

      # 7️⃣ Add Android Platform (COMPATIBLE)
      - name: Add Android Platform
        run: cordova platform add android@11.0.0

      # 8️⃣ Firebase config
      - name: Create google-services.json
        run: |
          mkdir -p platforms/android/app
          echo "$GOOGLE_SERVICES_JSON" > platforms/android/app/google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}

      # 9️⃣ Prepare Android
      - name: Prepare Android
        run: cordova prepare android

      # 🔟 Build APK (Gradle 7.6.4)
      - name: Build APK
        run: cordova build android --debug --no-interactive

      # 1️⃣1️⃣ Upload APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Homey-Vendor-APK
          path: platforms/android/app/build/outputs/apk/debug/app-debug.apk
